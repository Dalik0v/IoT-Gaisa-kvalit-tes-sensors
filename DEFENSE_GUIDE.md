# 🎓 Полное руководство для защиты проекта
## IoT Датчик качества воздуха

---

# 📌 1. ПРОЕКТ ПРОСТЫМИ СЛОВАМИ (Kopsavilkums)

## Что это такое?
Представь, что в комнате есть **невидимый датчик**, который как нос — он "нюхает" воздух и говорит тебе:
- **Сколько кислорода** (O₂) — хватает ли дышать?
- **Сколько CO₂** (углекислый газ) — не слишком ли душно?
- **Сколько пыли** (частицы) — чистый ли воздух?
- **Температура** — тепло или холодно?
- **Влажность** — сухо или влажно?

## Какую проблему решает?
🏠 **Проблема**: В помещениях часто душно, но мы этого не замечаем. Высокий CO₂ = головная боль, усталость, плохая концентрация.

✅ **Решение**: Датчик измеряет воздух каждые 10 секунд и показывает на сайте в реальном времени. Если плохо — мигают красные светодиоды!

## Как это работает? (Простая схема)

```
┌──────────────────┐      HTTPS         ┌─────────────────┐       SQL         ┌──────────────┐
│   📟 Датчик      │  ──────────────►  │   🖥️ Сервер     │  ◄──────────►     │  🗄️ База    │
│   (ESP32)        │                    │   (Node.js)     │                    │  (PostgreSQL)│
└──────────────────┘                    └─────────────────┘                    └──────────────┘
                                               │
                                               │ WebSocket
                                               ▼
                                        ┌─────────────────┐
                                        │   🌐 Сайт       │
                                        │   (React)       │
                                        │   Пользователь  │
                                        └─────────────────┘
```

**Простым языком:**
1. **Датчик** измеряет воздух → отправляет данные на сервер через интернет
2. **Сервер** получает данные → сохраняет в базу данных
3. **Сайт** получает данные с сервера → показывает красивые графики пользователю

---

# 📌 2. ЖЕЛЕЗО / HARDWARE (Ierīces daļa)

## Что внутри датчика?

| Компонент | Что это? | Зачем нужен? |
|-----------|----------|--------------|
| **ESP32** | Мозг датчика (микроконтроллер) | Управляет всем, подключается к WiFi |
| **SCD41** | Сенсор качества воздуха | Измеряет CO₂, температуру, влажность |
| **3 синих LED** | Синие светодиоды | Показывают уровень CO₂ (1, 2 или 3 горят) |
| **3 красных LED** | Красные светодиоды | Показывают уровень влажности |
| **1 зелёный LED** | Статусный светодиод | Мигает = работает |

## Схема подключения

```
ESP32 GPIO Пины:
├── I2C (для SCD41 сенсора)
│   ├── GPIO 21 → SDA (данные)
│   └── GPIO 22 → SCL (тактовый сигнал)
│
├── Синие LED (CO₂ уровень)
│   ├── GPIO 13 → LED 1 (низкий CO₂)
│   ├── GPIO 12 → LED 2 (средний CO₂)
│   └── GPIO 14 → LED 3 (высокий CO₂)
│
├── Красные LED (влажность)
│   ├── GPIO 27 → LED 1 (сухо)
│   ├── GPIO 25 → LED 2 (нормально)
│   └── GPIO 32 → LED 3 (влажно)
│
└── Статус LED
    └── GPIO 4 → Мигает (система работает)
```

## Как работает индикация LED?

### Синие LED (CO₂):
| Горит LED | CO₂ уровень | Что это значит? |
|-----------|-------------|-----------------|
| 1 из 3 | < 500 ppm | 🟢 Отлично! Свежий воздух |
| 2 из 3 | 500-800 ppm | 🟡 Нормально, можно проветрить |
| 3 из 3 | > 800 ppm | 🔴 Душно! Открой окно! |

### Красные LED (Влажность):
| Горит LED | Влажность | Что это значит? |
|-----------|-----------|-----------------|
| 1 из 3 | < 35% | 🔴 Слишком сухо |
| 2 из 3 | 35-55% | 🟢 Комфортно |
| 3 из 3 | > 55% | 🟡 Влажно |

### Статусный LED (зелёный):
| Скорость мигания | CO₂ | Значение |
|------------------|-----|----------|
| Медленно (1 сек) | < 500 | Всё хорошо |
| Средне (0.5 сек) | 500-800 | Внимание |
| Быстро (0.25 сек) | > 800 | Проветри! |

## Как подключается к интернету?

ESP32 подключается к WiFi и отправляет данные на сервер:

```
1. ESP32 включается
2. Подключается к WiFi (домашний роутер или iPhone hotspot)
3. Каждые 10 секунд измеряет воздух
4. Отправляет JSON на сервер через HTTPS:
   {
     "device_id": "AA:BB:CC:DD:EE:FF",  ← MAC адрес устройства
     "co2": 450,                         ← CO₂ в ppm
     "temperature": 22.5,                ← Температура °C
     "humidity": 45.3,                   ← Влажность %
     "oxygen": 20.8,                     ← Кислород %
     "particles": 13.5                   ← Частицы ug/m³
   }
```

---

# 📌 3. СЕРВЕР / BACKEND (Servera arhitektūra)

## Технологии

| Технология | Зачем? |
|------------|--------|
| **Node.js** | Среда выполнения JavaScript на сервере |
| **Express** | Веб-фреймворк для создания API |
| **TypeScript** | JavaScript с типами (меньше ошибок) ✨ BONUS |
| **Socket.IO** | WebSocket для реального времени |
| **PostgreSQL** | База данных для хранения измерений |

## Структура сервера

```
backend/
├── src/
│   ├── server.ts          ← Главный файл сервера
│   ├── config/
│   │   └── database.ts    ← Подключение к PostgreSQL
│   └── routes/
│       ├── measurements.ts ← API для измерений
│       ├── devices.ts      ← API для устройств
│       └── auth.ts         ← API для авторизации
```

## API Endpoints (Как общаться с сервером)

### 📥 Приём данных от датчика
```
POST /api/measurements
```
- **Что делает**: Датчик отправляет сюда свои измерения
- **Входные данные**: JSON с CO₂, температурой, влажностью
- **Что происходит**:
  1. Проверяет MAC адрес устройства
  2. Если устройство новое — автоматически регистрирует
  3. Сохраняет измерение в базу данных
  4. Отправляет данные всем подключённым сайтам через WebSocket

### 📤 Получение последнего измерения
```
GET /api/measurements/latest
```
- **Что делает**: Возвращает последнее измерение
- **Кто использует**: Сайт при загрузке страницы

### 📤 История измерений
```
GET /api/measurements/history?limit=50
```
- **Что делает**: Возвращает последние N измерений
- **Кто использует**: Сайт для построения графиков

### 📤 Список устройств
```
GET /api/devices
```
- **Что делает**: Возвращает все зарегистрированные датчики

### 📥 Добавление устройства
```
POST /api/devices
```
- **Входные данные**: MAC адрес, имя, локация
- **Что делает**: Регистрирует новый датчик в системе

## WebSocket — Реальное время! 🚀

**Проблема**: Если сайт будет каждую секунду спрашивать сервер "есть новые данные?" — это много запросов.

**Решение**: WebSocket — постоянное соединение. Сервер сам отправляет данные, когда они появляются!

```javascript
// На сервере (server.ts)
io.on('connection', (socket) => {
    console.log('Клиент подключился');
});

// Когда приходят новые данные
io.emit('newMeasurement', { co2: 450, oxygen: 20.8, ... });

// На сайте (websocket.ts)
socket.on('newMeasurement', (data) => {
    // Обновить графики!
});
```

---

# 📌 4. БАЗА ДАННЫХ (Datu bāzes struktūra - 3NF)

## Что такое 3NF? (Третья нормальная форма)

Представь, что у тебя много игрушек и тебе нужно их разложить по коробкам:
- **1NF**: В каждой ячейке только одна игрушка (не куча)
- **2NF**: Все игрушки полностью зависят от коробки (не половина)
- **3NF**: Игрушки не зависят друг от друга (машинка не зависит от куклы)

## Таблицы в базе данных

### Таблица `users` (Пользователи)
```sql
┌────────────────────────────────────────────────────┐
│  id  │    name    │      email       │  password  │
├──────┼────────────┼──────────────────┼────────────┤
│  1   │  Danila    │  dan@mail.com    │  ******    │
│  2   │  Student   │  stud@mail.com   │  ******    │
└────────────────────────────────────────────────────┘
- id = уникальный номер (PK = Primary Key)
- Каждый пользователь может иметь несколько датчиков
```

### Таблица `devices` (Устройства/Датчики)
```sql
┌──────────────────────────────────────────────────────────────────────┐
│ id │      device_id       │    name         │ location │ user_id (FK)│
├────┼──────────────────────┼─────────────────┼──────────┼────────────┤
│ 1  │ AA:BB:CC:DD:EE:FF    │ Гостиная        │ 1 этаж   │     1      │
│ 2  │ 11:22:33:44:55:66    │ Спальня         │ 2 этаж   │     1      │
└──────────────────────────────────────────────────────────────────────┘
- device_id = MAC адрес (уникальный идентификатор железа)
- user_id = FK (Foreign Key) → ссылка на таблицу users
```

### Таблица `measurements` (Измерения)
```sql
┌────────────────────────────────────────────────────────────────────────────────┐
│ id │ device_id (FK) │ oxygen │ co2  │ particles │ temperature │   created_at   │
├────┼────────────────┼────────┼──────┼───────────┼─────────────┼────────────────┤
│ 1  │       1        │ 20.85  │ 450  │   12.5    │    22.3     │ 2025-12-18 ... │
│ 2  │       1        │ 20.80  │ 480  │   13.0    │    22.4     │ 2025-12-18 ... │
│ 3  │       2        │ 20.90  │ 420  │   11.0    │    21.5     │ 2025-12-18 ... │
└────────────────────────────────────────────────────────────────────────────────┘
- device_id = FK → ссылка на таблицу devices
- Каждое измерение принадлежит одному датчику
```

## Связи между таблицами

```
users (1) ────────< devices (N)
  │                    │
  │ один пользователь  │ одно устройство
  │ много устройств    │ много измерений
  │                    │
              devices (1) ────────< measurements (N)
```

## Почему это 3NF? ✅

| Правило | Объяснение | Примечание |
|---------|------------|----------|
| **1NF** | Все значения атомарные | Нет массивов, нет повторяющихся групп |
| **2NF** | Нет частичных зависимостей | Все поля зависят от всего ключа |
| **3NF** | Нет транзитивных зависимостей | Поля не зависят друг от друга через другие поля |

**Пример соблюдения 3NF:**
- ❌ Плохо: В таблице measurements хранить `user_name`
- ✅ Хорошо: Получать имя пользователя через JOIN: measurements → devices → users

---

# 📌 5. ДОСТУП К СЕРВЕРУ (Cloudflare Tunnel)

## Проблема: Как сделать сервер доступным из интернета?

Твой компьютер находится за роутером. У него "серый" IP адрес (192.168.x.x), который не виден из интернета.

## Варианты решения:

| Способ | Плюсы | Минусы |
|--------|-------|--------|
| Статический IP | Просто настроить | Дорого (платить провайдеру) |
| Port Forwarding | Бесплатно | Небезопасно, нужен белый IP |
| **Cloudflare Tunnel** ✅ | Бесплатно, безопасно, HTTPS | Нужен аккаунт Cloudflare |

## Как работает Cloudflare Tunnel?

```
                           Cloudflare сеть
┌──────────────┐         ┌──────────────────┐         ┌──────────────┐
│ ESP32 датчик │ ──────► │ api.danilanet.id.lv │ ◄───── │ Пользователь │
└──────────────┘    ▲    └──────────────────┘         └──────────────┘
                    │            │
                    │            │ Tunnel (шифрованный)
                    │            ▼
                    │    ┌──────────────┐
                    │    │ Твой сервер  │
                    │    │ (localhost)  │
                    │    └──────────────┘
                    │            │
                    └── Никаких открытых портов!
```

**Что это даёт:**
- ✅ **HTTPS автоматически** — данные зашифрованы
- ✅ **Не нужен статический IP** — работает из-за NAT
- ✅ **DDoS защита** — Cloudflare фильтрует атаки
- ✅ **Красивый домен** — `api.danilanet.id.lv` вместо IP

## Конфигурация в проекте

```yaml
# docker-compose.yml
cloudflared:
  image: cloudflare/cloudflared:latest
  command: tunnel --config /etc/cloudflared/config.yml run
  volumes:
    - ./config.yml:/etc/cloudflared/config.yml
```

---

# 📌 6. DOCKER COMPOSE (Как всё запускается)

## Что такое Docker?

Представь, что Docker — это "коробка", в которой лежит всё нужное для программы:
- Сама программа
- Все библиотеки
- Правильные настройки

**Плюс**: Работает одинаково везде! На Windows, Linux, Mac...

## Что такое Docker Compose?

Это инструмент для запуска **нескольких Docker контейнеров** одной командой.

## Наши сервисы (4 контейнера)

```
┌────────────────────────────────────────────────────────────────────┐
│                        docker-compose.yml                          │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐             │
│  │   postgres   │  │   backend    │  │   frontend   │             │
│  │   База       │  │   Сервер     │  │   Сайт       │             │
│  │   данных     │  │   Node.js    │  │   React      │             │
│  │   :5432      │  │   :5000      │  │   :80        │             │
│  └──────────────┘  └──────────────┘  └──────────────┘             │
│         │                 │                 │                      │
│         └─────────────────┴─────────────────┘                      │
│                           │                                        │
│                    co2meter-network                                │
│                                                                    │
│  ┌──────────────┐                                                  │
│  │  cloudflared │  ← Туннель в интернет                           │
│  └──────────────┘                                                  │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
```

## Разбор docker-compose.yml

### 1. PostgreSQL (База данных)
```yaml
postgres:
  image: postgres:16-alpine     # Официальный образ PostgreSQL
  container_name: co2meter-postgres
  ports:
    - "5432:5432"               # Порт базы данных
  environment:
    POSTGRES_DB: co2meter       # Имя базы
    POSTGRES_USER: postgres     # Пользователь
    POSTGRES_PASSWORD: ...      # Пароль
  volumes:
    - postgres_data:/var/lib/postgresql/data  # Данные сохраняются!
  healthcheck:                  # Проверка что база готова
    test: ["CMD-SHELL", "pg_isready -U postgres"]
```

### 2. Backend (Node.js сервер)
```yaml
backend:
  build:
    context: ./backend          # Собрать из папки backend
  ports:
    - "8080:5000"               # Доступен на порту 8080
  environment:
    - DATABASE_URL=postgresql://postgres:...@postgres:5432/co2meter
  depends_on:
    postgres:
      condition: service_healthy  # Ждать пока база готова
```

### 3. Frontend (React сайт)
```yaml
frontend:
  build:
    context: ./frontend/my-react-app
  ports:
    - "80:80"                   # Стандартный HTTP порт
  depends_on:
    - backend                   # Ждать пока сервер запустится
```

### 4. Cloudflared (Туннель)
```yaml
cloudflared:
  image: cloudflare/cloudflared:latest
  command: tunnel --config /etc/cloudflared/config.yml run
  volumes:
    - ./config.yml:/etc/cloudflared/config.yml
  depends_on:
    - frontend
    - backend
```

## Как запустить весь проект?

```bash
# Одна команда — и всё работает!
docker-compose up -d --build

# -d = в фоновом режиме
# --build = пересобрать образы
```

---

# 📌 7. САЙТ / FRONTEND (WEB daļa)

## Технологии

| Технология | Зачем? |
|------------|--------|
| **React** | Библиотека для построения интерфейса |
| **TypeScript** | JavaScript с типами ✨ BONUS |
| **Canvas API** | Рисование графиков |
| **Socket.IO Client** | WebSocket для реального времени |

## Структура сайта

```
frontend/my-react-app/src/
├── App.tsx              ← Главный компонент, навигация
├── index.css            ← Глобальные стили
├── translations.ts      ← Переводы (LV/RU/EN) ✨ BONUS
├── components/
│   ├── Auth.tsx         ← Форма логина/регистрации
│   └── Profile.tsx      ← Профиль пользователя
├── pages/
│   ├── HomePage.tsx     ← Главная с графиками
│   ├── DevicesPage.tsx  ← Управление устройствами
│   └── SettingsPage.tsx ← Настройки
└── services/
    └── websocket.ts     ← Подключение к WebSocket
```

## Функционал сайта

### 🏠 Главная страница (HomePage)
- **Три карточки** с текущими значениями: O₂, CO₂, Частицы
- **Три графика** в реальном времени (Canvas)
- **Цветовые индикаторы** качества воздуха
- **Рекомендации** — если CO₂ высокий, показывает "Проветри!"

### 📱 Страница устройств (DevicesPage)
- **Список** всех зарегистрированных датчиков
- **Добавление** нового датчика (по MAC адресу)
- **Удаление** датчика
- **Статус** устройства (активно/неактивно)

### ⚙️ Настройки (SettingsPage)
- **Профиль** пользователя
- **Смена языка** (LV/RU/EN)
- **Тёмная/светлая тема**

## Как работают графики?

```javascript
// HomePage.tsx — Canvas рисование
const drawChart = (canvas, data, color) => {
  const ctx = canvas.getContext('2d');
  
  // 1. Очистить холст
  ctx.fillRect(0, 0, width, height);
  
  // 2. Нарисовать сетку
  for (let i = 0; i <= 5; i++) {
    ctx.moveTo(0, y);
    ctx.lineTo(width, y);
    ctx.stroke();
  }
  
  // 3. Нарисовать линию графика
  ctx.beginPath();
  data.forEach((point, index) => {
    const x = index * stepX;
    const y = normalizeValue(point.value);
    ctx.lineTo(x, y);
  });
  ctx.stroke();
  
  // 4. Добавить градиент под линией
  ctx.fill();
};
```

## Как добавить устройство?

```
1. Зайти на страницу "Devices" (Устройства)
2. Нажать кнопку "Add Device" (Добавить)
3. Ввести:
   - MAC адрес (например: AA:BB:CC:DD:EE:FF)
   - Название (например: "Гостиная")
   - Локация (например: "1 этаж")
4. Нажать "Save" (Сохранить)

MAC адрес показывается в Serial Monitor Arduino при запуске!
```

## Мультиязычность ✨ BONUS

```typescript
// translations.ts
export const translations = {
  lv: {
    home: { title: "Gaisa kvalitāte", ... },
    ...
  },
  ru: {
    home: { title: "Качество воздуха", ... },
    ...
  },
  en: {
    home: { title: "Air Quality", ... },
    ...
  }
};
```

---

# 📌 8. БОНУСНЫЕ ФИЧИ (Papildu)

## ✅ Что у тебя есть:

| Бонус | Статус | Где реализовано? |
|-------|--------|------------------|
| **TypeScript** | ✅ | Backend + Frontend (статическая типизация) |
| **WebSocket** | ✅ | Реальное время без перезагрузки страницы |
| **Мультиязычность** | ✅ | LV/RU/EN переключаются в настройках |
| **Тёмная тема** | ✅ | Переключается в настройках |
| **Responsīvs dizains** | ✅ | Работает на мобильных |
| **Графики** | ✅ | Canvas с реальными данными |
| **Авторизация** | ✅ | Login/Register с базой данных |

---

# 📌 9. ТЕХНИЧЕСКИЕ РЕШЕНИЯ И ВЫЗОВЫ (Tehniskie lēmumi)

## Принятые решения:

### 1. Почему ESP32, а не Arduino Uno?
- **ESP32** имеет встроенный WiFi
- Мощнее (больше памяти, быстрее)
- Поддерживает HTTPS

### 2. Почему PostgreSQL, а не MongoDB?
- Реляционная база = строгая структура
- Легче гарантировать 3NF
- Хорошо работает с числовыми данными (измерения)

### 3. Почему WebSocket, а не polling?
- **Polling**: сайт каждую секунду спрашивает "есть данные?" → много трафика
- **WebSocket**: сервер сам отправляет, когда есть данные → эффективно!

### 4. Почему Cloudflare Tunnel?
- Бесплатно
- Не нужен статический IP
- Автоматический HTTPS
- Защита от DDoS

## Вызовы (проблемы) и решения:

| Проблема | Решение |
|----------|---------|
| iPhone hotspot отключается | Добавлен авто-реконнект WiFi каждые 30 сек |
| MAC адрес не совпадает | Нормализация: все в uppercase, `:` вместо `-` |
| Сенсор не определяется | Добавлен simulation mode для тестирования |
| База не готова при старте | healthcheck + depends_on в Docker |

## Возможные улучшения:

1. **Mobile app** — iOS/Android приложение
2. **Push уведомления** — оповещения о плохом воздухе
3. **Больше сенсоров** — VOC, дым, температура наружу
4. **Статистика** — средние за день/неделю/месяц
5. **Экспорт данных** — CSV/PDF отчёты

---

# 📌 10. ЧТО ПОКАЗЫВАТЬ НА ДЕМО (Demonstrācija)

## Порядок демонстрации:

### 1. Показать железо (1-2 мин)
- "Вот датчик ESP32 с сенсором SCD41"
- "Видите, горят LED — синие для CO₂, красные для влажности"
- "Вот он мигает — это значит работает"

### 2. Показать Serial Monitor (30 сек)
- Открыть Arduino IDE → Serial Monitor
- "Смотрите, каждые 10 секунд приходят данные"
- "Вот MAC адрес устройства"

### 3. Показать сайт (2-3 мин)
- Открыть https://danilanet.id.lv
- "Вот главная страница с графиками"
- "Видите, данные обновляются в реальном времени"
- Перейти на Devices: "Вот список устройств"
- Переключить язык: "Работает на трёх языках"
- Переключить тему: "Есть тёмная тема"

### 4. Показать как данные идут (1 мин)
- "Датчик отправляет на api.danilanet.id.lv"
- "Сервер сохраняет в базу PostgreSQL"
- "И через WebSocket отправляет на сайт"

### 5. Ответить на вопросы

---

# 📌 11. ВОЗМОЖНЫЕ ВОПРОСЫ И ОТВЕТЫ

## Про железо:

**Q: Почему именно SCD41?**
A: Это точный CO₂ сенсор от Sensirion. Измеряет CO₂, температуру и влажность одновременно. Точность ±40ppm для CO₂.

**Q: Почему 3 уровня LED, а не плавно?**
A: Для простоты восприятия. Человеку легче понять "3 горит = плохо", чем смотреть на яркость.

**Q: Как часто отправляются данные?**
A: Каждые 10 секунд. Можно изменить в коде (SEND_INTERVAL).

## Про сервер:

**Q: Почему Node.js?**
A: Быстрый, асинхронный, хорошо работает с WebSocket. Большое сообщество.

**Q: Что если сервер упадёт?**
A: Docker автоматически перезапустит (restart: unless-stopped).

**Q: Как защищены данные?**
A: HTTPS шифрование, пароли хэшируются, JWT токены для авторизации.

## Про базу данных:

**Q: Почему 3NF важно?**
A: Избегаем дублирования данных. Если изменить имя устройства — меняем в одном месте.

**Q: Не будет ли база переполняться?**
A: Одно измерение ~100 байт. 1 миллион измерений = ~100 MB. Можно добавить автоочистку старых данных.

## Про сайт:

**Q: Почему не Chart.js?**
A: Canvas API даёт больше контроля. Графики кастомные и красивые.

**Q: Работает на телефоне?**
A: Да! Responsive дизайн, тестировался на iPhone и Android.

---

# 📌 ФИНАЛЬНЫЙ ЧЕКЛИСТ

## Перед защитой проверь:

### Железо:
- [ ] ESP32 заряжен / подключён к питанию
- [ ] WiFi работает (iPhone hotspot включён)
- [ ] LED горят / мигают правильно
- [ ] Serial Monitor показывает данные

### Сервер:
- [ ] `docker-compose up -d` работает
- [ ] Сайт открывается: https://danilanet.id.lv
- [ ] API работает: https://api.danilanet.id.lv/health

### Сайт:
- [ ] Графики обновляются в реальном времени
- [ ] Можно добавить/удалить устройство
- [ ] Переключение языка работает
- [ ] Тёмная тема работает

### Презентация:
- [ ] Есть схема архитектуры
- [ ] Есть скриншоты сайта
- [ ] Знаешь что показывать на демо
- [ ] Готов отвечать на вопросы

---

# 🎯 УДАЧИ НА ЗАЩИТЕ! 🚀

Помни:
- Говори спокойно и уверенно
- Не читай со слайдов — рассказывай
- Если не знаешь ответ — честно скажи "не уверен, но думаю что..."
- Покажи что ты понимаешь как работает система целиком

**Ты готов! 💪**
